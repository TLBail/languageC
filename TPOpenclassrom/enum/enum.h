#ifndef DEF_ENUM
#define DEF_ENUM

    typedef enum Difficulter Difficulter;
    enum Difficulter
    {
        FACILE = 1,
        MOYEN = 2,
        DIFFICILE = 3
    };

    #include "enum.c"

#endif